# Project Conventions
<!-- Auto-generated by /ss:init — auto-detected sections are regenerated each run -->

## Code Style

- No linter/formatter config present yet (no `.editorconfig`, checkstyle, or spotless plugin).
- Single source file so far (`src/main/java/com/cafeyokai/Main.java`) — conventions below are
  starting defaults, not yet observed patterns.

## Naming Conventions

- Root package: `com.cafeyokai`.
- Standard Java conventions expected: PascalCase classes, camelCase methods/fields.

## Project Patterns

- Gradle Kotlin DSL (`build.gradle.kts`, `settings.gradle.kts`) — keep new build config in
  Kotlin DSL, not Groovy.
- No modules split yet; SPEC.md calls for `core`, `android`, `desktop` LibGDX modules —
  expect `settings.gradle.kts` to grow multi-module `include(...)` entries as those land.

## Testing Conventions

- JUnit Jupiter 6, `useJUnitPlatform()` configured in `build.gradle.kts`.
- No test files exist yet — no established naming/location pattern to detect. Recommend
  `src/test/java/com/cafeyokai/...Test.java` mirroring main source layout.

## Git Conventions

- No commit history yet on this branch to derive a message style from.

## Custom Conventions

<!-- ss:user-additions -->
<!-- Add project-specific conventions below. Content here is preserved on /ss:init re-run. -->
<!-- ss:end -->
